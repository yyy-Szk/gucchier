<%= render 'pages/header' %>

<div class="container text-center text-white"> <!-- アカウントロックの解除メール再送信 -->

  <div class="row subtitle-margin">
    <div class="col-md-12">
      <h2>ロック解除メールの再送信</h2>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">

      <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
        <%= devise_error_messages! %>

        <div class="field">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "text-black" %>
        </div>

        <div class="actions">
          <%= f.submit "再送信", class: "text-black devise-submit" %>
        </div>

      <% end %>

      <%= render "devise/shared/links" %>

    </div>
  </div>
</div>
