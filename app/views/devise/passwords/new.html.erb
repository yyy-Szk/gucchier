<%= render 'pages/header' %>

<div class="container text-center text-white"> <!--　パスワード再設定フォーム -->

  <div class="rowsubtitle-margin">
    <div class="col-md-12">
      <h2>パスワード再設定</h2>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= devise_error_messages! %>

        <div class="field">
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "text-black" %>
        </div>

        <div class="actions">
          <%= f.submit "送信", class: "text-black devise-submit" %>
        </div>

      <% end %>

      <%= render "devise/shared/links" %>

    </div>
  </div>
</div>
